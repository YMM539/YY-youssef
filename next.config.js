/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com', // For project images
      'via.placeholder.com'   // For client logos
    ],
  },
};

module.exports = nextConfig